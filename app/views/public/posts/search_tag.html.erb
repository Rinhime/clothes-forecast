<h2>タグが<%=@tag.name%>の投稿一覧</h2>

  <!--タグリスト-->
  <% @tag_list.each do |list|%>
    <i class="fa-sharp fa-solid fa-tag"></i>
    <%=link_to post.tag_name,public_search_tag_path(tag_id: list.id) %>
    <%="(#{list.post.count})" %>
  <% end %>